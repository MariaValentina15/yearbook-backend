info:
  name: Mensagem - Deletar erro
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{baseUrl}}/mensagens/999"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 404", function() {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
