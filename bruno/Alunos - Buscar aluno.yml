info:
  name: Alunos - Buscar aluno
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/alunos/5"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 200 para aluno", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
