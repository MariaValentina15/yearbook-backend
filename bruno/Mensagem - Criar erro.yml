info:
  name: Mensagem - Criar erro
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/mensagens"
  body:
    type: json
    data: |-
      {
        "texto": "",
        "autorId": 1
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 400 sem texto", function() {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
